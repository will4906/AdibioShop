<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2017. willshuhua.
  -->

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.willshuhua.adibioshop.dao.CartDao">

    <select id="queryCartForCustomer" resultType="CartItem">
        SELECT cart_items.cart_id, cart_items.cart_itemid, cart_items.product_id, cart_items.quantity
        FROM cart_items, cart
        WHERE cart.customer_id = #{customerId}
        AND
        cart_items.cart_id = cart.cart_id
    </select>

</mapper>